<h1>Editing Book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <br>
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
    <br>
  <% end %>
<% end %>

<!-- form_with部分 -->
<%= form_with model:@book, local: true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <div>
  <%= f.submit 'Update Book' %>
  </div>
<% end %>

<span><%= link_to "Show", book_path(@book) %></span> <span> | </span>
<!--かっこの中はなんやった？-->
<a href="https://172ad8794d6749a7a274dff00c661a08.vfs.cloud9.us-east-1.amazonaws.com/books" rel="">Back</a>


